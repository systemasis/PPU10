int main (int argc,char argv[]){
	int nb_polling      = atoi(argv[1]);
	int delai_poll      = atoi(argv[2]);
	int n;
	int nb_req_rx;
	for(int i=0;i<=n;i++){
		switch(state){
			case 2: //w__req
				printf("Prim %s st%d %ds \n",string_state,i,delai_poll);
				
				
			break;
			case 5: // bc_data
				printf("Prim %s st%d \n",string_state,i);

			break;		
		}	
	}
}
