//traffic
int main (int argc,char argv[]){
	int i=artoi(argv[2]); //numero de la station secondaire
	int pid_Sti = artoi(argv[3]); // pid de la station secondaire
	// delai minimun et max entre deux requete pour une meme station
	int delai_min_requete = artoi(argv[4]); 	//>0
	int delai_max_requete = artoi(argv[5]);		//>=delai_min_requete
	
	switch(state){
		case:7

		break;
	}
}
